import * as React from "react";

interface IBoardRouteProps {}

const BoardRoute: React.FunctionComponent<IBoardRouteProps> = (props) => {
  return <div>Board to-do tasks</div>;
};

export default BoardRoute;
